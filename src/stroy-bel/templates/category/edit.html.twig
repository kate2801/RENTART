{% extends 'admin.html.twig' %}

{% block title %}Редактировать{% endblock %}

{% block body %}
<div class="container">
    <h1 class="h1 my-3 text-center">Редактировать</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Обновить'}) }}
    <div class="row text-center">
        <a href="{{ path('category_index') }}">Назад</a>

        {{ include('category/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
